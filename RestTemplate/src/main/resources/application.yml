server:
  port: 8020
spring:
  application:
    #   表示注册时的微服务名称
    name: consumer
eureka:
  client:
    service-url:
      #      表示注册中心的地址
      defaultZone: http://localhost:8761/eureka/
  #   是否将当前服务的 IP 注册到 Eureka Server。
  instance:
    prefer-ip-address: true