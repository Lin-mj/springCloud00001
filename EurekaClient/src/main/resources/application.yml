server:
  port: 8011
spring:
  application:
    #   表示注册时的微服务名称
    name: provider
eureka:
  client:
    service-url:
      #      表示注册中心的地址
      defaultZone: http://localhost:8761/eureka/
  #      表示是否注册
  instance:
    prefer-ip-address: true